package domain;

import javax.swing.*;

public class EpisodeList extends JFrame {
    private JList list1;
    private JPanel panel1;
    private JButton selectEpisodeBtn;

    public void buildForm(){
        add(panel1);

        setTitle("Account");
        setSize(600,600);
    }
}

